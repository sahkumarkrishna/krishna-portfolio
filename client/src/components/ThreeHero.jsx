import React, { useRef } from 'react';\nimport { Canvas, useFrame } from '@react-three/fiber';\nimport { Float, Box, Text3D, Center } from '@react-three/drei';\n\nconst FloatingLaptop = () => {\n  const laptopRef = useRef();\n  \n  useFrame((state) => {\n    if (laptopRef.current) {\n      laptopRef.current.rotation.y = Math.sin(state.clock.elapsedTime * 0.5) * 0.2;\n      laptopRef.current.position.y = Math.sin(state.clock.elapsedTime * 0.8) * 0.1;\n    }\n  });\n\n  return (\n    <Float speed={1} rotationIntensity={0.2} floatIntensity={0.5}>\n      <group ref={laptopRef}>\n        {/* Laptop Base */}\n        <Box position={[0, -0.1, 0]} args={[2, 0.2, 1.5]}>\n          <meshStandardMaterial color=\"#1f2937\" metalness={0.8} roughness={0.2} />\n        </Box>\n        {/* Laptop Screen */}\n        <Box position={[0, 0.5, -0.7]} args={[2, 1.2, 0.1]} rotation={[-0.2, 0, 0]}>\n          <meshStandardMaterial color=\"#111827\" metalness={0.9} roughness={0.1} />\n        </Box>\n        {/* Screen Content */}\n        <Box position={[0, 0.5, -0.65]} args={[1.8, 1, 0.05]} rotation={[-0.2, 0, 0]}>\n          <meshStandardMaterial color=\"#06b6d4\" emissive=\"#06b6d4\" emissiveIntensity={0.3} />\n        </Box>\n      </group>\n    </Float>\n  );\n};\n\nconst CodeSymbols = () => {\n  const symbols = ['<', '>', '{', '}', '(', ')', ';'];\n  \n  return (\n    <>\n      {symbols.map((symbol, index) => (\n        <Float key={index} speed={1 + index * 0.2} rotationIntensity={0.5}>\n          <Text3D\n            font=\"/fonts/helvetiker_regular.typeface.json\"\n            size={0.3}\n            height={0.1}\n            position={[\n              (Math.random() - 0.5) * 8,\n              (Math.random() - 0.5) * 6,\n              (Math.random() - 0.5) * 4\n            ]}\n          >\n            {symbol}\n            <meshStandardMaterial color=\"#3b82f6\" transparent opacity={0.7} />\n          </Text3D>\n        </Float>\n      ))}\n    </>\n  );\n};\n\nconst ThreeHero = () => {\n  return (\n    <div className=\"absolute inset-0 pointer-events-none\">\n      <Canvas camera={{ position: [0, 0, 6], fov: 60 }}>\n        <ambientLight intensity={0.4} />\n        <pointLight position={[5, 5, 5]} intensity={0.6} color=\"#06b6d4\" />\n        <pointLight position={[-5, -5, 5]} intensity={0.4} color=\"#3b82f6\" />\n        \n        <FloatingLaptop />\n        <CodeSymbols />\n      </Canvas>\n    </div>\n  );\n};\n\nexport default ThreeHero;